# LifeCache v3 — Full-Frame Oracle for Long-Horizon Review Prompts
# Captures clean full-frame K/V from early frames and injects
# deterministically into later frames of the same scene.
# 120 frames, 3 frames per block, 40 blocks.

lifecache:
  enabled: true
  trace_only: false
  mode: union

  compression: none
  recall_enabled: true
  recall_top_sets: 1
  recall_top_tokens: 1560  # One full frame
  max_frame_distance: null

  anchor_enabled: false
  motion_enabled: false

  region_bias_beta: 0.0
  trace_path: cache_trace.jsonl
  record_latency: true

  enable_last_n_layers: 1

  rope_safe_recall: true
  allow_post_rope_recall: false
  rope_remap_policy: relative_clamp
  max_post_rope_frame_distance: 21
  capture_clean_only: false
  use_real_query_for_compression: false

  # Oracle: capture first frame, recall at intervals throughout generation
  # Frame 0 = initial scene state, recalled at frames 30, 60, 90
  oracle_mode: full_frame
  oracle_layer: 29
  oracle_num_frames: 1
  oracle_capture_frames: [0]
  oracle_recall_frames: [30, 60, 90]
  oracle_append_mode: true

  head_roles_path: third_party/Pyramid-Forcing/configs/head_configs/best_labels.csv

  bank_max_compressed_sets: 4
  bank_max_compressed_tokens: 8192
